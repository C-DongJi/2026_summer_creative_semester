# --- Core DL ---
# GPU(CUDA) 빌드 설치:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# CPU 전용(파이프라인 개발/테스트):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=2.1
torchaudio>=2.1

# --- Audio I/O & DSP ---
soundfile>=0.12
librosa>=0.10
numpy>=1.24
scipy>=1.11
# MP3 디코딩에 시스템 ffmpeg 필요 (conda: environment.yml에 포함)

# --- 모델/학습 프레임워크 ---
# 주력 모델(Mel-Band RoFormer)은 MSST 체크아웃으로 로드:
#   bash scripts/setup_msst.sh   (MSST 자체 requirements 포함 설치)
einops>=0.7
tqdm>=4.66
pyyaml>=6.0
tensorboard>=2.15
# demucs>=4.0   # 레거시 HTDemucs 비교 실험 시에만

# --- Web UI ---
gradio>=4.0

# --- Dev ---
pytest>=7.4
